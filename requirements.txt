# Runtime deps for local data generation and evidence capture.
# Glue jobs run on Glue 5.0 (Spark 3.5 / Python 3.11) and do NOT use this file.
#
# Faker is deliberately absent: name generation is done from fixed word lists
# seeded by random.Random so the dataset is byte-identical across machines and
# across library upgrades. Reproducibility beats realism here.
boto3>=1.34
pandas>=2.2
pyarrow>=15.0
